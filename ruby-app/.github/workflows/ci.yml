name: CI

on: pull_request

jobs:
  ci-tests:
    name: Run Test Suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup CI container
        run: bin/test
